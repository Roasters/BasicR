print("Hello")

# ctrl + l : Clear

install.packages("randomForest")

# NA : Scalar(No value)

is.na(NA)

# NULL: empty basket, the status where a variable is declared but not assigned any specific value.
iseven <- NULL


# factor data: categorical data(data is categorized based on predetermined type)
# - nominal factor : No comparison of size between data
# - ordinal factor : Size comparison possible(A+, A, B+ ...)
# numeric data: literally numeric data type(Score, temperature ...)

sex <- factor("f", c("m", "f"))
# initialized as "f" among "m" and "f"

